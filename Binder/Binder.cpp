// Binder.cpp : Defines the entry point for the application.
//
#include "Binder.h"

Binder::Binder(ExecutableFile* initialExecutable, ExecutableFile* targetExecutable) : m_initialExecutable(initialExecutable), m_targetExecutable(targetExecutable), m_resultExecutable()
{
    //bind();
}

void Binder::bind()

{
}